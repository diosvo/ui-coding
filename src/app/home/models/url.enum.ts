enum EUrl {
  COMPONENT = 'component-ui',
  WEB = 'web-ui',
  FUNCTION = 'function'
}

enum EComponentUI {
  BUTTON = 'button',
  CARD = 'card',
  MENU = 'menu'
}

enum EWebUI {
  SLIDER = 'slider',
}

export {EUrl, EComponentUI, EWebUI};